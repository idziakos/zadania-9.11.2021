"Każda klasa posiada specjalną funkcję constructor()," 
"która jest automatycznie odpalana przy tworzeniu nowej instancji za pomocą słowa new."

function PersonFn(name, age) {
    this.name = name;
    this.age = age;
}